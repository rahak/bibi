version: 2.1

jobs:
  distribute:
    docker:
      - image: circleci/node:12.4.0
    working_directory: ~/pegasus
    steps:
        - checkout
        - run: npm install
        - run: npm run distribute
        - store_artifacts:
            path: ./archives

workflows:
  build:
    jobs:
      - distribute
